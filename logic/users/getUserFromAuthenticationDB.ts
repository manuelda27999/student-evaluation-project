/* EN OBRAS */

/* import * as admin from "firebase-admin";

admin.initializeApp();

const getUserWithEmailFromAuthenticationDB = async (email: string) => {
  try {
    const user = await admin.auth().getUserByEmail(email);
    console.log("User retrieved successfully:", user.uid);
    return user;
  } catch (error) {
    console.error("Error trying to get user with email:" + error);
  }
};

export default getUserWithEmailFromAuthenticationDB; */
