/* import { faker } from "@faker-js/faker";
import createAspect from "../../aspects/createAspect";
import admin from "../../../firebase/firebaseAdmin";
import { auth } from "../../../firebase/credentials";
import { signOut } from "firebase/auth";

describe("createAspect (with random data)", () => {
  test("create aspect successfully with random data", async () => {
    const randomName = faker.word.words(2);
  });

  afterAll(async () => {
    await signOut(auth);
  });
});
 */
